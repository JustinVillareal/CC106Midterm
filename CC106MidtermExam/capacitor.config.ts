import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CC106MidtermExam',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
